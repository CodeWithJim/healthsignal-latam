# Copia este archivo como .env y reemplaza únicamente el valor de la clave.
# .env está ignorado por Git y se carga sólo en el backend.
OPENAI_API_KEY=pega_aqui_tu_clave_de_openai

# Opcional. La aplicación usa este modelo si la variable no está definida.
OPENAI_MODEL=gpt-5.6-terra
